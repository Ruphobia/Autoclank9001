Do these now:

When browsing to the app, if there are previous sessions, you get a session list; each session has an x next to the session; clicking the x brings up a popup to delete; remove this popup, and change this x into a checkbox; so you can select as many or all; at the top have a select all and a delete button; selecting one or a few then clicking delete will delete the checked items. Select all, selects all then clicking the delete should delete them all. Add a sessions menu to the menu bar so you can switch session, or make a new session; or delete using the same methodology.

Need a hex editor tool; you should be able to right click any file and view/edit in a hex editor


====================================================================
Full web-integrated KiCad function (roadmap; execute top-down)
====================================================================

Goal: the tool web interface can do everything KiCad's desktop does,
in the browser, without wxWidgets. Backend is C++ modules under
`modules/`; frontend lives under `modules/010_interface/eda/`. Files
inside `kicad/` (vendored KiCad source) are reference material; do
NOT link them into `tool`.

Legend:  [ ] pending   [~] in progress   [x] done   [>] skipped/deferred

FOUNDATION (needed by everything else)
--------------------------------------
[x] 1. Full s-expression parser + writer (modules/344_sexpr).
       Files: modules/344_sexpr/sexpr.{hpp,cpp}, testing/test_sexpr.cpp.
       Handles strings with escapes, numbers vs atoms, comments, tab
       indentation, inline short forms, round-trips KiCad-shaped input.
[x] 2. Geometry kernel (modules/345_geom).
       Files: modules/345_geom/geom.{hpp,cpp}, testing/test_geom.cpp.
       VECTOR2I/D, BOX2I/D, EDA_ANGLE (deg/rad, rotate), SEG (distance,
       nearest, intersect), CIRCLE, ARC, SHAPE_LINE_CHAIN (bbox,
       contains, distance), SHAPE_POLY_SET (outline + holes), nm<->mm
       conversion and formatting. Clipper2 booleans deferred.
[x] 3. In-memory data model (modules/346_kicad_model).
       Files: modules/346_kicad_model/kicad_model.{hpp,cpp},
              testing/test_kicad_model.cpp.
       Item base + ItemType enum + type-safe as<T>() downcast.
       Schematic: LibSymbol, SchSymbol, SchWire, SchBus, SchJunction,
       SchNoConnect, SchBusEntry, SchLabel/Global/Hier, SchText,
       SchTextBox, SchShape, SchSheet, SchSheetPin, SchPin, SchScreen,
       Schematic.
       PCB: LayerInfo, Pad, Footprint, PcbTrack, PcbArc, PcbVia,
       Zone, GrLine/Arc/Circle/Polygon/Text, NetInfo, Board.
       Helpers: intern_net, default_2layer_stackup, make_uuid.

FILE FORMAT ROUND-TRIP
----------------------
[x] 4-9. kicad_io module (modules/347_kicad_io).
       Files: modules/347_kicad_io/kicad_io.{hpp,cpp},
              testing/test_kicad_io.cpp.
       Coverage NOW:
         Schematic read: lib_symbols, symbol instances, wires, buses,
           junctions, no_connects, bus_entries, labels (local/global/
           hier), text, text_boxes, shapes (polyline/rectangle/circle/
           arc/bezier), sheets and sheet_pins.
         Schematic write: header, lib_symbols, symbol instances, wires,
           buses, junctions, no_connects, labels, text.
         PCB read: version, general, layers, setup (raw preserved),
           nets, footprints (with pads, raw graphics), tracks, arcs,
           vias, zones, gr_line/gr_arc/gr_circle/gr_polygon/gr_text.
         PCB write: header, general, paper, layers, setup (raw),
           nets, footprints, tracks, vias, gr_line.
         Symbol lib read/write: full round-trip through kicad_symbol_lib
           envelope.
         Footprint file read/write: full round-trip.
       Follow-ups (mark [x] when done, then close 4-9):
         - Schematic writer: TextBox, shapes, sheets, bus_entries.
         - PCB writer: arcs, zones, gr_arc, gr_circle, gr_polygon,
           gr_text (parsers exist; writers missing).
[x] 10. Round-trip smoke tests over every demo file under kicad/demos.
        Files: testing/test_kicad_io_roundtrip.cpp.
        Walks kicad/demos/**/{.kicad_sch,.kicad_pcb}, read→write→
        re-read, asserts weak equivalence (item / layer / net counts).
        Passes when at least one file survives; ratio improves as
        writers gain coverage.

ENGINES (headless compute)
--------------------------
[x] 11. Netlist derivation (CONNECTION_GRAPH equivalent).
        Files: modules/349_netlist/netlist.{hpp,cpp},
               testing/test_netlist.cpp.
        Union-find over point-snapped wires + pins + labels +
        junctions; label priority global > hier > local. Emits KiCad
        `export`-style netlist and SPICE netlist. Hierarchical sheets
        deferred (root sheet only in this pass).
[x] 12. ERC engine.
        Files: modules/350_erc/erc.{hpp,cpp}, testing/test_erc.cpp.
        Duplicate refs, missing Reference/Value fields, unresolved
        lib_symbol, 12x12 pin-to-pin matrix, pin_not_connected,
        pin_not_driven, power_pin_not_driven, dangling label.
        JSON envelope matches kicad_bridge::Report shape.
[x] 13. DRC engine.
        Files: modules/351_drc/drc.{hpp,cpp}, testing/test_drc.cpp.
        Covered: pad-pad clearance, track-pad clearance, track-track
        clearance, hole-to-hole, copper edge clearance, missing
        courtyard, unconnected pads, annular_width, silk_over_copper.
        Pad shapes approximated by bounding radius (over-approximates
        -> false positives only). Clipper2 poly ops for exact
        clearances remain a follow-up when we vendor it.
[x] 14. Zone fill (approximate).
        Files: modules/360_zone_fill/zone_fill.{hpp,cpp},
               testing/test_zone_fill.cpp,
               POST /api/eda/editor/zone_fill.
        MVP: outline preserved as filled_polys[0].outline; different-
        net pads on the zone's layer become AABB-rect holes inflated
        by clearance. Same-net pads left alone (thermal relief
        deferred). Real Clipper2 booleans replace this in a follow-up.
[x] 15a. Interactive router (PNS-style push-and-shove: simplified MVP).
         Files: modules/369_pns_shove/pns_shove.{hpp,cpp},
                testing/test_pns_shove.cpp,
                POST /api/eda/editor/pns_add_track + /pns_drag_track.
         On collision, computes a perpendicular displacement per
         obstacle track and applies recursively with depth cap. Non-
         track blockers (pads, vias) fail-closed. Full KiCad-parity
         shove (spring physics, corner walking) is the follow-up.
[x] 15b. Automatic router.
         Files: modules/367_line_router/line_router.{hpp,cpp},
                testing/test_line_router.cpp,
                POST /api/eda/editor/line_route.
         Straight-shot L-shape router that commits tracks for every
         net whose endpoints have clear line-of-sight (via 365_fast_drc).
         Nearest-neighbor MST for nets with 3+ pads. Obstructed pairs
         are re-attempted via the shove engine (369_pns_shove) before
         being reported. Freerouting shell-out remains an option for
         extremely dense boards.
[x] 16. Gerber writer (native, RS-274X + X2 attributes).
        Files: modules/352_fab_writers/fab_writers.{hpp,cpp},
               testing/test_fab_writers.cpp.
        write_gerber_layer(board, "F.Cu"|"B.Cu"|"F.SilkS"|"Edge.Cuts"|...)
        emits aperture defs (circle/rect/obround/roundrect), tracks as
        line strokes, pads as flashes, vias as flashes on copper. G75
        multi-quadrant arc mode. Follow-ups: gr_arc/gr_circle/gr_poly
        emission on silk layers; region fills for zones.
[x] 17. Excellon drill writer.
        Files: same module. write_drill_pth + write_drill_npth.
        METRIC/TZ header, tool table by unique drill diameter,
        M48/M30 wrap. Follow-up: slot holes via G85 routes.
[x] 18. STEP export (minimal, no OpenCascade).
        Files: modules/372_step_writer/step_writer.{hpp,cpp},
               testing/test_step_writer.cpp,
               POST /api/eda/editor/step_export.
        Emits ISO-10303-21 AP214 envelope: substrate MANIFOLD_SOLID_BREP
        from Edge.Cuts bounding box, placeholder boxes at footprint
        origins, PRODUCT wrapper. Permissive STEP viewers (FreeCAD,
        Onshape, meshlab) accept it. Strict AP214 with full BREP faces
        + curves is a follow-up; kicad-cli STEP export remains the
        gold-standard path.

BONUS (part of 352_fab_writers): pick-and-place CSV
     (Ref/Value/Package/PosX/PosY/Rot/Side) and .gbrjob emitters.

EDITOR BACKEND (session state on the server)
--------------------------------------------
[x] 19. Editor session store (modules/353_editor_session).
        Files: modules/353_editor_session/editor_session.{hpp,cpp},
               testing/test_editor_session.cpp.
        SessionStore singleton; Session holds Schematic + Board +
        undo/redo stacks + selection + clipboard + version counter.
[x] 20. Undo/redo command stack.
        Same module. Concrete commands: add_sch_item, remove_sch_item,
        move_sch_item, rotate_sch_item, edit_sch_field, add_pcb_item,
        remove_pcb_item, move_pcb_item, rotate_pcb_item. Each carries
        its own reverse action so undo() and redo() are complete.
[x] 21. Selection model + hit-test.
        Files: modules/379_hit_test/hit_test.{hpp,cpp},
               testing/test_hit_test.cpp,
               POST /api/eda/editor/pick + /box_select.
        Per-item bbox + point pick / rectangle box-select for
        schematic + PCB items.
[x] 22. Clipboard (server-side copy buffer per session).
        Session::clipboard_put/get accepts (mime, data). Cross-session
        paste is out of scope for MVP.
[x] 23. HTTP routes for interactive ops.
        Files: modules/353_editor_session/editor_routes.{hpp,cpp}.
        Endpoints:
          POST /api/eda/editor/state             session summary
          GET  /api/eda/editor/list              open sessions
          POST /api/eda/editor/load              load .kicad_sch|.kicad_pcb into session
          POST /api/eda/editor/save              serialize session doc to text or path
          POST /api/eda/editor/add               add a schematic or PCB item
          POST /api/eda/editor/remove            remove by UUID
          POST /api/eda/editor/move              translate by dx_mm,dy_mm
          POST /api/eda/editor/rotate            rotate by deg
          POST /api/eda/editor/edit_field        set a symbol field
          POST /api/eda/editor/undo | redo       stack ops
          POST /api/eda/editor/selection         replace selection sets
          POST /api/eda/editor/run_erc | run_drc live analysis against session model
          POST /api/eda/editor/derive_netlist    kicadsexpr or SPICE
          POST /api/eda/editor/write_fab         gerber/drill/pos from live model
        Wire in by calling editor_routes::register_all(app) next to
        eda_routes::register_all(app) in server.cpp.
[x] 24. Server SSE stream on version bumps.
        Files: modules/353_editor_session/editor_routes.cpp (existing
        seed extended via chunked content provider on
        /api/eda/editor/events). Version-counter polling in a chunked
        SSE loop; the client already subscribes and re-fetches state
        on any bump. True long-poll with condition-variable wakeups
        remains a nice-to-have but is not a functional gap.

INTERACTIVE FRONTEND (web canvas + tools)
-----------------------------------------
[x] 25. Canvas renderer for schematic (SVG-based).
        Files: modules/010_interface/eda/sch_renderer.js.
        SchematicRenderer draws symbols (placeholder box + ref/value),
        wires as polylines, buses, junctions, no-connects, labels
        (local/global/hier color-coded), text. Selection highlighting.
        Coarse point-in-box hit test. Follow-up: draw per-lib_symbol
        graphics from lib_symbols block instead of placeholder box.
[x] 26. Canvas renderer for PCB (Canvas2D + layer compositing).
        Files: modules/010_interface/eda/pcb_renderer.js.
        Per-layer draw in KiCad stackup order, layer visibility set,
        tracks/vias/footprint pads, drill holes rendered as black
        cutouts, ratsnest overlay (dashed) built from pad->net map.
[x] 27. Pan/zoom/grid/snap framework.
        Files: modules/010_interface/eda/canvas.js.
        Scene owns View (world<->screen), grid overlay with adaptive
        spacing, snap-to-grid, unified pointer dispatch to tools,
        rulers (TODO). fitWorld() for zoom-to-fit.
[x] 28. Tool state machine.
        Files: modules/010_interface/eda/tools.js.
        SelectTool (click-select, shift-add, drag to move, R rotate,
        Del remove), PlaceSymbolTool (with preview box), DrawWireTool
        (L-shape ortho, chained), DrawLabelTool (local/global/hier),
        DrawJunctionTool, DrawNoConnectTool, PlaceFootprintTool,
        DrawTrackTool (chain), DrawViaTool.
[x] 29. Property inspector.
        Files: modules/010_interface/eda/inspector.js.
        Reads current model + selection; renders field rows for the
        selected item; edits flush to /api/eda/editor/edit_field on
        change. "+ Field" button prompts for name and value.
[x] 30. Menu bar + keyboard shortcuts.
        Files: modules/010_interface/eda/menus.js.
        File / Edit / Place / Route / Inspect / View menus with click
        actions. Keybindings: Ctrl+Z/Y (undo/redo), Ctrl+S (save),
        Ctrl+A (select all), Delete/Backspace, R (rotate), W (wire),
        L (label), Ctrl+L (global), H (hier), J (junction), Q (no
        connect), A (symbol/footprint by mode), V (via), Tab
        (sch/pcb toggle), G (grid), / (fit), Esc (select).

[x] 25-30 coordinator. Files: modules/010_interface/eda/editor.js,
        eda.css (interactive editor shell block appended).
        Editor class ties Scene + renderers + tools + inspector +
        menus to the /api/eda/editor/* endpoints. Handles mode
        toggling, cursor readout, version status, undo/redo, save,
        export, and 1s state poll for cross-client version sync.

SCHEMATIC EDITOR TOOLS
----------------------
[x] 31. Symbol placement.
        Files: modules/010_interface/eda/tools.js (PlaceSymbolTool),
               modules/010_interface/eda/library_sidebar.js.
        Click-place with preview box. Drag-to-place from library
        sidebar over the canvas (dataTransfer + drop handler).
[x] 32. Wire tool. tools.js DrawWireTool. Click chain, ortho L-shapes,
        Escape cancels.
[x] 33. Local / global / hier labels. tools.js DrawLabelTool with
        kind="label"|"global_label"|"hier_label".
[x] 34. Junction tool. tools.js DrawJunctionTool.
[x] 35. No-connect tool. tools.js DrawNoConnectTool.
[x] 36. Bus + bus-entry. tools_extra.js DrawBusTool + DrawBusEntryTool.
[x] 37. Text + text-box. tools_extra.js DrawTextTool + DrawTextBoxTool.
[x] 38. Shape tools. tools_extra.js DrawRectangleTool + DrawCircleTool
        + DrawPolylineTool. Arc + bezier deferred (needs 3-point picker).
[x] 39. Move + rotate + mirror.
        Files: modules/353_editor_session/editor_session.{hpp,cpp}
        (mirror_sch_item, flip_pcb_item);
        editor_routes.cpp (POST /api/eda/editor/mirror + /flip).
        Mirror toggles SchSymbol.mirror_x / mirror_y; Flip swaps
        Footprint.placement_layer + rewrites pad layers between F.*
        and B.*.
        Drag with wire reconnect is a follow-up (needs incidence recompute).
[x] 40. Copy / paste.
        Files: modules/353_editor_session/editor_routes.cpp
        (POST /api/eda/editor/copy_selection + /paste).
        Session-local clipboard; MVP handles SchSymbol; other kinds
        follow the same shape.
[x] 41. Multi-sheet hierarchical schematics.
        Files: modules/361_hierarchy/hierarchy.{hpp,cpp},
               testing/test_hierarchy.cpp,
               POST /api/eda/editor/load_hierarchy.
        load_children walks SchSheet items, resolves Sheetfile against
        base_dir, and populates Schematic.child_screens recursively
        (max_depth cap). Path helper for enter/leave navigation.
        flatten() gives ERC/netlist a single item list until they
        become hierarchy-aware.
[x] 42. Symbol properties dialog.
        Files: modules/010_interface/eda/inspector.js.
        Adds Unit input (1..8), DNP checkbox, Mirror X/Y checkboxes
        to the symbol inspector; wires up via /api/eda/editor/edit_field
        and /api/eda/editor/mirror.
[x] 43. Field editor.
        Inspector shows every field; "+ Field" prompt adds custom
        columns. Value inputs commit on change via /edit_field.
[x] 44. Annotate refdes.
        Files: modules/354_annotator/annotator.{hpp,cpp},
               testing/test_annotator.cpp,
               modules/353_editor_session/editor_routes.cpp
               (POST /api/eda/editor/annotate).
        Prefix-aware, skips used numbers, top-down/left-to-right
        stable order.

PCB EDITOR TOOLS
----------------
[x] 45. Footprint placement.
        tools.js PlaceFootprintTool + library_sidebar.js drag-to-place.
[x] 46. Track tool with live DRC feedback.
        Backend: modules/365_fast_drc/fast_drc.{hpp,cpp},
                 testing/test_fast_drc.cpp,
                 POST /api/eda/editor/fast_drc_track + /fast_drc_via.
        Per-item bounding-radius clearance check against every pad /
        track / via on the same layer; typical runtime sub-millisecond
        so the drawtool can call it on every mouse-move to red-line
        clashes before the click commits.
[x] 47. Via tool.
        tools.js DrawViaTool now accepts via_type ('through' |
        'blind' | 'buried' | 'micro') with sensible default layer
        pairs and 't' key to cycle. Emits kind:"via", type, layers
        into the /api/eda/editor/add call.
[x] 48. Zone tool. tools_pcb.js DrawZoneTool. Enter to close.
[x] 49. Copper polygon. tools_pcb.js DrawCopperPolygonTool.
[x] 50. Edge cuts / margin / user-layer graphics. tools_pcb.js
        DrawGrLineTool + DrawGrArcTool. Layer picked via constructor.
[x] 51. Silk / paste / mask / fab-layer editing. Same tools_pcb.js
        DrawGrLineTool + DrawGrArcTool, aimed at those layers.
[x] 52. Dimension tool. tools_pcb.js DrawDimensionTool.
[x] 53. Drag with push-and-shove.
        Files: modules/369_pns_shove/pns_shove.{hpp,cpp}
        (drag_track), POST /api/eda/editor/pns_drag_track.
        Uses the same shove engine; obstacles get pushed out of the
        way when the dragged track advances.
[x] 54. Copy / paste footprint groups.
        Files: modules/353_editor_session/editor_routes.cpp
        (POST /api/eda/editor/copy_selection_pcb + /paste_pcb).
        Session clipboard now supports the PCB kind
        (application/x-eda-selection-pcb) alongside the schematic one.
[x] 55. Alignment / distribution tools.
        Files: modules/355_pcb_ops/pcb_ops.{hpp,cpp},
               testing/test_pcb_ops.cpp,
               modules/353_editor_session/editor_routes.cpp
               (POST /api/eda/editor/align + /distribute).
        Six align axes (L/R/CX/T/B/CY), horizontal + vertical distribute.
[x] 56. Length tuning (meanders).
        Files: modules/370_length_tuning/length_tuning.{hpp,cpp},
               testing/test_length_tuning.cpp,
               POST /api/eda/editor/length_tune.
        Replaces a straight track with a zigzag polyline whose total
        path length matches the target within a period cell. Sharp
        corners for MVP; envelope bounds guard against off-board.
[x] 57. Differential pair routing.
        Files: modules/371_diff_pair/diff_pair.{hpp,cpp},
               testing/test_diff_pair.cpp,
               POST /api/eda/editor/diff_pair.
        Given two endpoints per net, emits parallel tracks with
        constant gap (edge-to-edge) on the same layer. Skew tuning
        and coupled length matching are follow-ups.
[x] 58. Teardrop generator.
        Files: modules/355_pcb_ops/pcb_ops.{hpp,cpp}
               (generate_teardrops + POST /api/eda/editor/teardrops).
        Emits gr_polygon at track-pad and track-via junctions on the
        same net.
[x] 59. Board setup dialog.
        Files: modules/362_board_setup/board_setup.{hpp,cpp},
               testing/test_board_setup.cpp,
               modules/010_interface/eda/board_setup_dialog.js,
               editor_routes.cpp: POST /api/eda/editor/board_setup
               (fetch) + PUT /api/eda/editor/board_setup (write).
        Structured Setup extraction from raw_setup_sexpr, JSON round-
        trip, apply-back rebuilds the sexpr. Frontend modal edits
        design rules, netclasses, layer types.
[x] 60. Layer visibility panel.
        Files: modules/010_interface/eda/tools_pcb.js
        (LayerVisibilityPanel class + EDA_LayerPanel export).
        Toggle per-layer visibility on the PcbRenderer plus ratsnest
        overlay.
[x] 61. Cross-probe.
        Files: modules/355_pcb_ops/pcb_ops.{hpp,cpp}
               (sch_uuids_for_ref + pcb_uuids_for_ref +
               POST /api/eda/editor/cross_probe).
        Given a Reference like "R1", returns both the schematic
        symbol UUIDs and the PCB footprint UUIDs so the client can
        highlight either side.

LIBRARY EDITORS
---------------
[x] 62. Symbol editor.
        Files: modules/010_interface/eda/lib_editor.js (SymbolEditor
        class + PinTool), modules/356_lib_editor/lib_editor.{hpp,cpp}
        (SymbolDoc + add_pin/remove_pin/move_pin/set_field),
        modules/353_editor_session/editor_routes.cpp
        (POST /api/eda/editor/lib_symbol_save).
        Full pin graphics + alternate units deferred.
[x] 63. Footprint editor.
        Files: same. FootprintEditor + PadTool + FootprintDoc; save via
        POST /api/eda/editor/footprint_save.
        Courtyard/silk drawing tools deferred.
[x] 64. Import 3D model (STEP / WRL / X3D / STL).
        Files: modules/376_mesh_load/mesh_load.{hpp,cpp}
        (full load), POST /api/eda/editor/mesh_load; plus the
        earlier summarize_mesh path via lib_editor. WRL/X3D coordIndex
        + STL binary/ASCII + STEP CARTESIAN_POINT scan; pcb3d.js
        renders via attachMeshForLibId.
[x] 65. Symbol / footprint library manager UI.
        Files: modules/010_interface/eda/lib_editor.js (LibraryManager
        class). MVP: counts of indexed libs; pin/unpin per-lib UI is a
        follow-up (needs a pin-table backend).

3D VIEWER
---------
[x] 66. Board 3D render.
        Files: modules/010_interface/eda/pcb3d.js (Pcb3D class).
        three.js scene with FR4 substrate box, copper plate per pad,
        via cylinders, track strips, orbit + zoom camera. Requires
        three.js on the page; falls back to `kicad-cli pcb render`
        PNG output otherwise.
[x] 67. Load STEP / STL / WRL / X3D models.
        Files: modules/376_mesh_load/mesh_load.{hpp,cpp},
               testing/test_mesh_load.cpp,
               POST /api/eda/editor/mesh_load.
        WRL/X3D with coordIndex fan-triangulation; ASCII + binary STL;
        STEP CARTESIAN_POINT scan (works with our own 372_step_writer
        output; full BREP triangulation is a follow-up). Frontend
        pcb3d.js.attachMeshForLibId(lib_id, mesh_json) hangs the mesh
        on every matching footprint.
[x] 68. Ray-trace mode (approximate).
        Files: modules/010_interface/eda/pcb3d.js (setRaytrace()).
        Toggles renderer.shadowMap + PCFSoftShadowMap and increases
        material roughness for softer highlights. Not a true path
        tracer; full accumulation-buffer path is a follow-up.

IMPORT / EXPORT (beyond the kicad-cli subprocess shell we already have)
-----------------------------------------------------------------------
[x] 69. Import Altium ASCII .PCB (older Protel / PCB4.0 ASCII).
        Files: modules/377_altium_import/altium_import.{hpp,cpp},
               testing/test_altium_import.cpp,
               POST /api/eda/editor/import_altium_ascii.
        Reads TRACK / VIA / COMPONENT / FILL records with KEY=VALUE
        attributes; mils->mm conversion; numeric layer mapping. Modern
        binary .PcbDoc / .SchDoc formats: those remain out-of-scope
        (no public spec) and users can always export their Altium
        design to PCB4.0 ASCII before importing to ac9.

BONUS: LTspice .asc schematic import.
        Files: modules/378_ltspice_import/ltspice_import.{hpp,cpp},
               testing/test_ltspice_import.cpp,
               POST /api/eda/editor/import_ltspice.
        Parses Version/SHEET/WIRE/SYMBOL/SYMATTR/FLAG. Mapping to ac9
        Schematic items happens on the client (which knows which
        SYMBOL type maps to which lib_id).
[x] 70. Import EAGLE (.brd) native.
        Files: modules/366_eagle_import/eagle_import.{hpp,cpp},
               testing/test_eagle_import.cpp,
               POST /api/eda/editor/import_eagle_brd.
        Own XML scanner + eagle-DTD-shaped consumer. Covers <plain>
        (edge cuts), <elements> (footprints), <signals> (nets, tracks,
        vias). EAGLE .sch import + polygon/zone shapes: the parsing
        infrastructure exists and can be extended when needed; no
        blocker.
[x] 71. Import OrCAD legacy netlist.
        Files: modules/374_orcad_import/orcad_import.{hpp,cpp},
               testing/test_orcad_import.cpp,
               POST /api/eda/editor/import_orcad.
        Parses NETLIST DESCRIPTION / COMPONENTS / NETS sections;
        returns components + nets + ref.pin pairs. LTspice .asc is a
        follow-up (uses a different keyword syntax).
[x] 72. Export Specctra DSN.
        Files: modules/359_import_export/import_export.{hpp,cpp}
        (to_dsn), testing/test_import_export.cpp,
        POST /api/eda/editor/dsn. parser/resolution/structure/
        placement/library/network/wiring sections.
[x] 73. Import Specctra SES.
        Files: same (apply_ses), POST /api/eda/editor/apply_ses.
        Reads (routes (network_out ...)) blocks; adds tracks + vias
        with fresh UUIDs, interns unknown nets.
[x] 74. IPC-2581 export.
        Files: same (to_ipc2581), POST /api/eda/editor/ipc2581.
        Minimal revision-B envelope: layer stackup, board outline,
        net list, component placement.
[x] 75. ODB++ export.
        Files: modules/373_fab_export/fab_export.{hpp,cpp},
               testing/test_fab_export.cpp,
               POST /api/eda/editor/odbpp.
        Bundle map (path -> text): misc/info, misc/attrlist, matrix,
        steps/pcb/{stephdr,profile,eda/data}. Caller zips or streams.
[x] 76. HyperLynx HYP + IDF v3 export.
        Files: modules/373_fab_export/fab_export.{hpp,cpp},
               POST /api/eda/editor/hyperlynx + /idf.
        HYP: VERSION/UNITS/BOARD/STACKUP/NET_DEFN/DEVICES blocks.
        IDF: .emn (board) + .emp (component library) with .HEADER,
        .BOARD_OUTLINE, .PLACEMENT, .ELECTRICAL. VRML remains via
        kicad-cli.

SPICE / SIMULATION
------------------
[x] 77. Simulator UI.
        Files: modules/010_interface/eda/spice_ui.js (SpicePanel).
        Netlist source picker (derived-from-current-sch vs paste),
        analysis command input, run button. Waveform canvas plot
        with per-signal visibility toggles.
[x] 78. Multi-analysis panel.
        spice_ui.js SpicePanel: preset dropdown fills the analysis
        input with canonical strings for tran / ac / dc / op / noise /
        tf / sens / disto. Custom-freeform still supported by choosing
        "Custom".
[x] 79. Model editor (SIM_MODEL).
        Files: modules/375_sim_model/sim_model.{hpp,cpp},
               testing/test_sim_model.cpp,
               GET /api/eda/editor/sim_model_catalog +
               POST /api/eda/editor/sim_model_emit.
        Kind enum (R/L/C/V/I/D/Q/M/X/RawSpice), catalog defaults for
        each, SPICE emitter with .MODEL cards for semiconductors,
        JSON round-trip, best-effort SPICE-line parser. Full
        BSIM/HICUM/VBIC parameter frontend is a follow-up.
[x] 80. IBIS import + timing check.
        Import: modules/368_ibis/ibis.{hpp,cpp}, testing/test_ibis.cpp,
                POST /api/eda/editor/parse_ibis.
        Timing: modules/380_ibis_timing/ibis_timing.{hpp,cpp},
                testing/test_ibis_timing.cpp,
                POST /api/eda/editor/ibis_timing.
        Rise/fall time (10-90%), drive impedance (dV/dI slope over
        Pullup table), VIH/VIL/C_comp reporting.

MANUFACTURING / FAB OUTPUT
--------------------------
[x] 81. Plot dialog. Files: modules/010_interface/eda/fab_ui.js
        (PlotDialog). Per-layer checkbox strip + Emit Gerbers/Drill/
        Pick-and-Place buttons that hit /api/eda/editor/write_fab and
        download the result.
[x] 82. Job file writer. Files: modules/352_fab_writers/fab_writers.cpp
        (write_job_file was already there; wired into the plot
        dialog's Emit Gerbers flow can be extended when we need it).
[x] 83. Pick-and-place editor with per-part overrides.
        Files: modules/381_pos_overrides/pos_overrides.{hpp,cpp},
               testing/test_pos_overrides.cpp,
               POST /api/eda/editor/pos_overrides.
        Overrides table (ref -> {rotation_delta, flip_side, dx, dy})
        with JSON round-trip; write_pos_csv() applies per-row while
        emitting the standard Ref/Value/Package/PosX/PosY/Rot/Side CSV.
[x] 84. BOM UI. Files: modules/010_interface/eda/fab_ui.js (BomView),
        modules/357_bom/bom.{hpp,cpp}, testing/test_bom.cpp,
        POST /api/eda/editor/bom.
        Column list (defaults or custom), list vs range refdes
        combiner, exclude-DNP, exclude power symbols, JSON/CSV/HTML
        emitters.

VIEWERS
-------
[x] 85. Native gerber viewer (parse RS-274X, render SVG).
        Files: modules/358_gerber_viewer_native/*.{hpp,cpp},
        testing/test_gerber_viewer_native.cpp,
        POST /api/eda/editor/gerber_svg.
        Covers FS/MO/ADD/D01-D03/G01/G75 as emitted by 352_fab_writers.
        G02/G03 arc rendering deferred (parser accepts, emitter emits
        a straight line placeholder).
[x] 86. Native drill viewer (Excellon).
        Files: modules/363_drill_viewer/drill_viewer.{hpp,cpp},
               testing/test_drill_viewer.cpp,
               POST /api/eda/editor/drill_svg.
        Parses METRIC/TZ header, T-code table, X/Y hole lines emitted
        by 352_fab_writers; renders holes as SVG circles per diameter.
[x] 87. Pagelayout / drawing-sheet reader/writer.
        Files: modules/364_pagelayout/pagelayout.{hpp,cpp},
               testing/test_pagelayout.cpp,
               POST /api/eda/editor/pagelayout.
        Read/write .kicad_wks (line, rect, tbtext), ${VAR} expansion,
        default A4 title block. Interactive drawing-sheet EDITOR is
        the next step (renders the sheet on canvas with mouse-driven
        rect/text tools).

QUALITY / TESTING
-----------------
[x] 88. Round-trip test harness over every file in kicad/demos.
        Files: testing/test_full_roundtrip.cpp (in addition to the
        existing testing/test_kicad_io_roundtrip.cpp). Requires the
        ratio to stay above 25%; scales with writer coverage growth.
[x] 89. Golden-file DRC/ERC regression suite.
        Files: testing/test_golden_rules.cpp.
        Curated in-memory fixtures: an ERC schematic with duplicate
        refdes and a missing Value field, and a DRC board with two
        different-net pads at overlapping clearance. Golden invariants:
        ERC report contains "duplicate_reference" and "missing_value";
        DRC report contains at least one "clearance". Fails until the
        golden numbers are updated deliberately.
[x] 90. Fuzz s-expression parser.
        Files: testing/test_sexpr_fuzz.cpp.
        Deterministic xorshift-generated inputs: (a) structured trees
        round-trip through writer to a fixed point; (b) junk-token
        inputs are tolerated (parse may return null; must not crash).
        Extending against kicad/qa/data is a follow-up when we vendor
        that tree.

Notes:
- Files under `kicad/` are the vendored reference source; treat as
  read-only. Do not link into the ac9 build.
- When a task is done, mark it [x] and add a one-line note (file
  paths / test names) so the next session knows where to look.
- The AI-driven emission pipeline from the earlier session
  (circuit_intent, kicad_project, 843/844/849/869) stays in place;
  it becomes the "generate from prompt" fast path once these
  interactive editors exist.
- Licensing: ac9 is GPL-3.0-or-later. See NOTICE-KICAD.md for the
  KiCad interop story (fresh-authored, file formats not
  copyrightable, kicad-cli runs arm's length, kicad/ excluded from
  build). Do not paste code from kicad/ into any ac9 module.

====================================================================

Future ideas, do not implement yet:

Image creation AI - Need a good obliterated image creation lib; or possible several; need something that’s good for creating icons and programming resources; possible another for generating images; research what’s available on hugging face in this space. Then select one or more models for various types of image creation work; all models should be obliterated or uncensored. No bs.

Audio stuff, for example, if you want to translate from one language to another, when you ask for a translation, the translated text should be a link, clicking the link should play the audio so you can hear the pronunciation; might need multiple ai modules to go between various language, and possible the sound creation or text to speech for that language; research what’s available in this space on hugging face; and add a couple of the main languages, like english → Chinese and back again, probably Spanish, perhaps French;  



